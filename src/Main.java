public class Main {/*
    public static void main(String[] args) {
        try {
            // Erstellen des ChatSenders mit der IP-Adresse und dem Port des Dozenten
            ChatSender sender = new ChatSender("IP_Adresse_des_Dozenten", 5000);

            // Erstellen einer ChatMessage mit dem gewünschten Inhalt
            ChatMessage message = new ChatMessage("Hallo, das ist eine Testnachricht!");

            // Senden der Nachricht und Empfangen der Antwort
            MessageResponse response = sender.sendMessage(message);

            // Überprüfen, ob die Nachricht erfolgreich gesendet wurde
            if (response.isSuccess()) {
                System.out.println("Nachricht erfolgreich gesendet!");
            } else {
                System.out.println("Fehler beim Senden der Nachricht.");
            }

            // Schließen des ChatSenders
            sender.close();

        } catch (Exception e) {
            e.printStackTrace();
        }
    }*/
}
